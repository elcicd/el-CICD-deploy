apiVersion: v2
name: elcicd-chart
description: |
  An all-in-one chart meant with the ability to define variables and deployment profiles in values.yml files.
keywords:
- values.yml
- parameterized
- profiles
- deployment profiles
- variables

type: application

version: "0.1.0"

dependencies:
- name: elcicd-lib
  version: 0.1.0
  repository: file://../elcicd-lib